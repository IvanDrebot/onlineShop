import { Injectable } from '@angular/core';
import {BehaviorSubject} from 'rxjs';

@Injectable({
  providedIn: 'root'
})

export class BehaviorService {
  // private orders = new BehaviorSubject<object>();
  // order = this.orders.asObservable();
  //
  //
  // constructor() { }
}
